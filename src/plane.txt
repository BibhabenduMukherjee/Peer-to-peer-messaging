Design peer to peer messaging app node- > node






sender  ---- reciever


1 . time of sender node in start to spin up
2 . register this node to the cluster of server
    
     --- get the ip of any server in the cluster
     --- push sender address and name 
     --- server can keep a track of records 
     --- all the senders , recievers
3. Let's assume that a seed server has some 
   user information 

4. Need to create the logic so that any user
   lookup the destination ip is present on the 
   nearest seed server storage
